all the function calls are listed in the shell scripts:

run_feature_selection.sh # for performing feature selections on both datasets
run_clustering.sh # perform clustering algorithms on new datasets
run_nn.sh # perform nn on new datasets
run_clu_nn.sh # perform nn on new datasets with clustering labels